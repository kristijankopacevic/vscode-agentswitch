name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: node scripts/check-no-secrets.js
      - run: npm run build
      - run: npx vsce package
      - uses: actions/upload-artifact@v4
        with:
          name: agentswitch-vsix-${{ matrix.os }}
          path: '*.vsix'
